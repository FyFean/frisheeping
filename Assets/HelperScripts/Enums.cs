public class Enums
{
  public enum SheepState
  {
    idle,
    walking,
    running
  }

  public enum DogState
  {
    idle,
    walking,
    running
  }

  public enum DogBehaviour {
    controls,
    strombom,
    arc_v1,
    arc_v2
  }
}